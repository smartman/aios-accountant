# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "accounting"

[setup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

SOURCE="$CODEX_SOURCE_TREE_PATH"
TARGET="$CODEX_WORKTREE_PATH"

cd "$TARGET"

if [ ! -e "$SOURCE/.env.local" ]; then
  echo "Source .env.local not found: $SOURCE/.env.local"
  exit 1
fi

ln -sfn "$SOURCE/.env.local" .env.local
echo "Symlinked .env.local -> $SOURCE/.env.local"

npm install
'''
