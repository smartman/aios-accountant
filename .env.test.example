# Copy to .env.test.local for local unit-test overrides.
# Unit tests only load .env.test.local and .env.test.

DATABASE_URL=postgresql://accounting:accounting@localhost:5432/accounting?schema=public
CREDENTIAL_ENCRYPTION_KEY=vitest-accounting-encryption-secret
